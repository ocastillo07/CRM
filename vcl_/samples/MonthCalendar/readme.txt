This sample shows you to use the MonthCalendar component.

If shows you how to get the date selected by the user in a PHP event, and
also how to react to date changes using javascript.

Also shows how to set the date programmatically, how to set the first day of the
week and how to toggle the ability to allow the user select a time.