Use Image to display a graphical image on a form. TImage introduces several 
properties to determine how the image is displayed within the boundaries 
of the TImage object.

This example features properties such as Imagesource to add an picture to
the image object. Proportional feature is showed too, used to scale the image
in a proportional fashion. Other properties used are the Stretch feature
and the link.