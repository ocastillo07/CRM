ListBox is usually used to display a scrollable list of items that users can
select, add, or delete.

In this example how to add items to the listbox . Use the edit box to write the
item text you want to add and the press the Add item button and it will appear
inserted in the listbox at the end of it.