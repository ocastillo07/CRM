This sample it doesn't contain a Page, but it features how you can use a Canvas
object to generate graphics easily.

It uses javascript to generate all instructions to perform all drawing operations.
