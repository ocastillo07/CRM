DBGrid control is a grid that presents data from a database.

This example connects to a oscommerce database and shows data in the datagrid.

Notice that you will have to change databasename information to make it work.