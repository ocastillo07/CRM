This example features storeproc among other database compones like
datasource, query and database.

A combobox with available stored procedures is shown and a button
to execute the procedure. When called data is presented in a dbgrid.