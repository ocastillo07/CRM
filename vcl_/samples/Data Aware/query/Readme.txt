Query component is used to access database data using a sql to filter 
the retrieving of it.

This example presents 2 combo boxes with dates you can use to filter
the data presentation and narrow the query.

Press the Filter Query button to efectively retrieve records.