This example features the IBStoredQuery class. 

This example shows the retrieving of data using a IBQuery and presentation on
a dbgrid. The DataSource component is introduced too.

Be sure to select correct database path in the DatabaseName property
of the Database component.
