This example features the IBTable component. 

This example shows the retrieving of data using a IBTable component
and presentation on a dbgrid. The DataSource component is introduced too.

Be sure to select correct database path in the DatabaseName property
of the Database component.
