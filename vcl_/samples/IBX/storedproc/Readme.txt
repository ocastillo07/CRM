This example features the IBStoredProc class. 

This example show how to access to a stored proc that lives in a
Interbase database. The retrieving of data and presentation on
a dbgrid is showed too.

Be sure to select correct database path in the DatabaseName property
of the Database component.
