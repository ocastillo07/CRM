This sample shows how to create a master detail with two grids, you need to set
MasterSource and MasterFields properties with the appropiate values and also set
the UseAjax property of the Page to true, as what we are doing is, when the row
changes on the Master grid, call the server to updated the Detail grid. 
